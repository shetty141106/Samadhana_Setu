This is a Spring Boot (Controller → Service → Repository → Entity) + React/Vite frontend.
Enum values are case-sensitive (e.g. IssuePriority is CRITICAL/HIGH/MEDIUM/LOW, uppercase).
Before making changes, read readme/remaining.md — it is the current source of truth for known gaps.
After completing a task, update the relevant checkbox in readme/remaining.md.
Do not modify readme/PROJECT_CONTEXT.md.