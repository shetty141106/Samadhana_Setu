[build]
  base = "frontend"
  command = "rm -rf node_modules && npm install --include=dev --prefer-online --package-lock=false && node node_modules/vite/bin/vite.js build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
